{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/:dim(\\d+x\\d+)", "destination": "/api/render?dim=:dim" },
    { "source": "/:dim(\\d+)", "destination": "/api/render?dim=:dim" }
  ]
}
